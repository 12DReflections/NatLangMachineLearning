# This collection uses nltk natural language library

To download all NLTK libraries for windows:
python -m nltk.downloader all

To download all NLTK libraries on an ubuntu server:
sudo python -m nltk.downloader -d /usr/local/share/nltk_data all 

If using Window's this library is best used with anaconda